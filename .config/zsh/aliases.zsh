

## Aliases

alias ll='ls -la'
alias gs='git status'
alias gp='git pull'
alias gc='git commit -m'

### Kubernetes
[ -f ~/.config/zsh/aliases/.kubectl_aliases ] && source ~/.config/zsh/aliases/.kubectl_aliases